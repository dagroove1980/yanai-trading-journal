# Google Apps Script Web App URL (required for Sheet sync & fetch)
# Get this from: Sheet → Extensions → Apps Script → Deploy → Manage deployments
APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
